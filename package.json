{
  "name": "ens-text-resolver",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dimensiondev/ens-text-resolver",
    "create-local": "graph create --node http://localhost:8020/ dimensiondev/ens-text-resolver",
    "remove-local": "graph remove --node http://localhost:8020/ dimensiondev/ens-text-resolver",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dimensiondev/ens-text-resolver"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
